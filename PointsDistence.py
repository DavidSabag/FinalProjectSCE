def PointsDistence(avgpoints, points, dis):
    for i in range(0, 4):
        if abs(avgpoints[i][0]-points[i][0])>dis or  abs(avgpoints[i][0]-points[i][0])>dis:
            return False
    return True

